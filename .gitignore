# Dependencies
node_modules/

# Build output (regenerated by `npm --prefix app run build`)
app/dist/

# Tooling logs / local state — never commit (see CLAUDE.md)
firebase-debug.log
*-debug.log
.vercel/
.claude/launch.json

# OS / editor cruft
.DS_Store
