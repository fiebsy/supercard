{
  "name": "supercard-app",
  "private": true,
  "version": "3.1.0",
  "type": "module",
  "description": "React render path for Supercards — the repo-access counterpart to the standalone HTML in docs/. Deploys to Vercel.",
  "scripts": {
    "dev": "vite",
    "spec": "node scripts/build-spec.mjs",
    "spec:check": "node scripts/build-spec.mjs --check",
    "validate": "node scripts/validate-v3-1.mjs",
    "build": "node scripts/build-spec.mjs && tsc && vite build && node scripts/copy-renders.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
